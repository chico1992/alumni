{% extends 'base.html.twig' %}

{% block title %} CV Library {% endblock %}

{% block body %}
	<div class="container">
		{% if cvs is empty %}
			<p>No CV found in the application</p>
		{% else %}
			<div class="row">
                {% for post in posts %}
                    <div class="col-md-4">
                        <div class="card mb-4 shadow-sm">
                            <object type="application/pdf" width="30%" height="50%" data="{{ path('download_document', { 'document': cv.getId()}) }}">
                            <div class="card-body">
                                <a data="{{ path('download_document', { 'document': cv.getId()}) }}">Details</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>

		{% endif %}
	</div>

{% endblock %}