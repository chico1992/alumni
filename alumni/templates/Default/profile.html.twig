{% extends 'base.html.twig' %}

{% block title %}Your profile{% endblock %}

{% block body %}
	<table class="table">
		<thead>
			<tr>
				<th scope="col"></th>
				<th scope="col">User information</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td>Username :</td>
				<td>{{ user.getUsername() }}</td>
			</tr>
			<tr>
				<td>Firstname :</td>
				<td>{{ user.getFirstname() }}</td>
			</tr>
			<tr>
				<td>Lastname :</td>
				<td>{{ user.getLastname() }}</td>
			</tr>
			<tr>
				<td>Email :</td>
				<td>{{ user.getEmail() }}</td>
			</tr>
			<tr>
				<td>CV :</td>
				<td>{{ user.getCv() }}</td>
			</tr>
			<tr>
				<td>Photo :</td>
				{% if user.getProfilePicture() is empty %}
					<td></td>
				{% else %}
					<td><img src="{{ path('download_picture', { 'document': user.getProfilePicture().getId() } ) }}" alt="profile picture" /></td>
				{% endif %}
			</tr>
		</tbody>
	</table>

<a href="{{ path("profile_edit") }}" class="btn btn-primary btn-lg" role="button">Edit profile</a>
<a href="{{ path("cv_edit") }}" class="btn btn-primary btn-lg" role="button">Upload CV</a>


{% endblock %}
